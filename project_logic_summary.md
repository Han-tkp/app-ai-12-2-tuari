# สรุปตรรกะและกระบวนการทำงานของ DropDetect (Migration Guide)

เอกสารนี้เน้นที่ **กระบวนการทำงาน (Workflow)**, **ค่ามาตรฐานทางเทคนิค (WHO Standards)** และ **ฟีเจอร์หลัก** ของโปรเจกต์ เพื่อใช้สำหรับการ Migrate และทำความเข้าใจ Logic

---

## 1. กระบวนการทำงานหลัก (System Workflow)

1.  **การรับภาพและวิเคราะห์เบื้องต้น (Reception & Pre-processing):**
    *   รับภาพจากกล้องจุลทรรศน์ในระดับ 30 FPS สำหรับแสดงผลลัพธ์ทันที
    *   คัดเลือกเฟรมภาพตามช่วงเวลา (Interval) เพื่อส่งเข้าสู่ระบบ AI เพื่อลดภาระการประมวลผล
2.  **การตรวจจับละอองหยด (Detection & Post-processing):**
    *   AI ระบุตำแหน่งและวาดขอบเขต (Bounding Box) ของละออง
    *   ใช้ระบบ **NMS** เพื่อตัดความซ้ำซ้อนของกรอบตรวจจับที่ทับซ้อนกัน
    *   คำนวณขนาดเส้นผ่านศูนย์กลางในหน่วยพิกเซล (Pixel Diameter) จากค่าเฉลี่ยความกว้างและสูงของกล่อง
3.  **การติดตามและนับจำนวน (Tracking & Session Management):**
    *   ใช้ระบบ **Centroid Tracking** เพื่อตรวจสอบว่าละอองที่ตรวจเจอคือ "ตัวเดิม" หรือ "ตัวใหม่"
    *   ถ้าเป็นตัวเดิม จะอัปเดตตำแหน่งปัจจุบัน แต่ถ้าเป็นตัวใหม่ จะเก็บสถิติเข้าสู่ Session หลัก
    *   รองรับฟังก์ชัน **Ignore** เพื่อเลือกไม่นับละอองบางตัวที่อาจเป็นสิ่งสกปรก
4.  **การวิเคราะห์ทางสถิติ (Statistical Analysis):**
    *   แปลงขนาดพิกเซลเป็นหน่วยไมครอน (µm) ตามกำลังขยายของเลนส์ (Calibration)
    *   คำนวณค่า Spread Factor และสถิติปริมาตรตามมาตรฐานสากล
5.  **การแสดงผลและรายงาน (Presentation & Export):**
    *   แสดงค่า VMD (Dv0.5) และ Span แบบเรียลไทม์บนหน้าจอ
    *   เมื่อเสร็จสิ้น สามารถส่งออกเป็นไฟล์ Excel ที่มีข้อมูลละอองทุกตัวพร้อมกราฟวิเคราะห์

---

## 2. ค่ามาตรฐานและสูตรคำนวณ (Mathematical Standards)

โปรเจกต์นี้อ้างอิงมาตรฐานการทดสอบเครื่องพ่นละอองฝอยจาก **WHO** โดยมี Logic หลักดังนี้:

### 2.1 สูตรการแปลงขนาด (Spread Factor Calculation)
เนื่องจากละอองที่ตกกระทบบนแผ่นสไลด์ (MgO Slides) จะแบนลง จึงต้องคูณด้วย Spread Factor เพื่อหาขนาดจริง:
*   `True Diameter = Crater Diameter * Spread Factor`
*   **เกณฑ์การใช้ SF (MgO):**
    *   เส้นผ่านศูนย์กลาง > 20 µm: SF = **0.86**
    *   15 ถึง 20 µm: SF = **0.80**
    *   10 ถึง 15 µm: SF = **0.75**
    *   น้อยกว่า 10 µm: SF = **0.70**

### 2.2 การคำนวณปริมาตร (Volume Calculation)
*   ใช้สัดส่วนทรงกลม: `Volume = (π / 6) * TrueDiameter^3`

### 2.3 ค่าคงที่มาตรฐาน WHO (Evaluation Limits)
*   **VMD Target:** ช่วง 10.0 ถึง 30.0 µm
*   **SPAN Target:** ต้องไม่เกิน 2.0
*   **Minimum Count:** ต้องนับละอองอย่างน้อย 200 ตัวเพื่อให้ผลสถิติน่าเชื่อถือ

---

## 3. รายการฟีเจอร์หลัก (Key Features)

1.  **AI Auto-Detection:** ตรวจจับละอองอัตโนมัติแบบ Real-time พร้อมวาดขอบ (Contour) รอบละออง
2.  **Multi-Tool Manual Annotation:** ระบบวาดด้วยมือ (Rectangle, Line, Ellipse) เพื่อวัดขนาดกรณี AI ตรวจไม่ถึง พร้อมแปลงหน่วยเป็นไมครอนทันที
3.  **Interactive Workspace:** ระบบ Zoom และ Pan เพื่อตรวจสอบความชัดเจนของละอองในระดับพิกเซล
4.  **Ghost Box Sub-Sampling:** กำหนดพื้นที่วิเคราะห์ (Region of Interest) เพื่อเจาะจงเฉพาะจุดที่ต้องการ
5.  **Adaptive Hardware Profiles:** ระบบปรับประสิทธิภาพอัตโนมัติตามสเปคเครื่อง (Low/Mid/High) เพื่อป้องกัน UI ค้าง
6.  **Excel Export:** รายงานสรุปข้อมูลละอองทุกตัว พร้อมค่าสถิติ Dv0.1, Dv0.5, Dv0.9 และ Span อย่างละเอียด
7.  **Auto-Save & Recovery:** ระบบสำรองข้อมูลโครงการอัตโนมัติ ป้องกันข้อมูลหายระหว่างการวิเคราะห์หน้างาน
8.  **Calibration Management:** ระบบจดจำค่ากำลังขยายเลนส์ (4x, 10x) และปรับอัตราส่วน µm/px แยกกันได้อิสระ
